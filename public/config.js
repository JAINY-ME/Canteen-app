// Configuration for split deployments (e.g., frontend on Netlify, backend on Render/Railway)
// Uncomment and customize the lines below if you split your frontend and backend deployments.
// By default, the app automatically detects the server if hosted together on Render, Railway, or a VPS.

/*
window.APP_CONFIG = {
    API_URL: "https://your-backend-server.onrender.com/api",
    STAFF_API_URL: "https://your-backend-server.onrender.com/api/staff",
    SOCKET_URL: "https://your-backend-server.onrender.com"
};
*/
